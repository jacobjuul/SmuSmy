<% if notice %>
<p id="notice"><%= notice%></p>
<% end %>

<div style="margin-left:auto; margin-right: auto;">

<% cache ['store', Product.latest] do %>
	<% @products.each do |product| %>
		<% cache ['entry', product] do %>
    
		  <div style="display:inline-block; width:236px">
		    <%= image_tag(product.image_url, {style:"height:292px"}) %>
        
		    <h3 class="font-century-gothic"><%= link_to product.title, product %></h3>
		    
        <%#= sanitize(product.description) %>
        
		    <div>
		      <span class="font-century-gothic"><%= number_to_currency(product.price, locale: :en, unit: "DKK ", separator: ",") %></span>
		      <%#= button_to 'Add to Cart', line_items_path(product_id: product) %>
		    </div>
        
		  </div>

		  <div style="display:inline-block; width:236px">
		    <%= image_tag(product.image_url, {style:"height:292px"}) %>
        
		    <h3 class="font-century-gothic"><%= link_to product.title, product %></h3>
		    
        <%#= sanitize(product.description) %>
        
		    <div>
		      <span class="font-century-gothic"><%= number_to_currency(product.price, locale: :en, unit: "DKK ", separator: ",") %></span>
		      <%#= button_to 'Add to Cart', line_items_path(product_id: product) %>
		    </div>
        
		  </div>

		  <div style="display:inline-block; width:236px">
		    <%= image_tag(product.image_url, {style:"height:292px"}) %>
        
		    <h3 class="font-century-gothic"><%= link_to product.title, product %></h3>
		    
        <%#= sanitize(product.description) %>
        
		    <div>
		      <span class="font-century-gothic"><%= number_to_currency(product.price, locale: :en, unit: "DKK ", separator: ",") %></span>
		      <%#= button_to 'Add to Cart', line_items_path(product_id: product) %>
		    </div>
        
		  </div>

		  <div style="display:inline-block; width:236px">
		    <%= image_tag(product.image_url, {style:"height:292px"}) %>
        
		    <h3 class="font-century-gothic"><%= link_to product.title, product %></h3>
		    
        <%#= sanitize(product.description) %>
        
		    <div>
		      <span class="font-century-gothic"><%= number_to_currency(product.price, locale: :en, unit: "DKK ", separator: ",") %></span>
		      <%#= button_to 'Add to Cart', line_items_path(product_id: product) %>
		    </div>
        
		  </div>

		  <div style="display:inline-block; width:236px">
		    <%= image_tag(product.image_url, {style:"height:292px"}) %>
        
		    <h3 class="font-century-gothic"><%= link_to product.title, product %></h3>
		    
        <%#= sanitize(product.description) %>
        
		    <div>
		      <span class="font-century-gothic"><%= number_to_currency(product.price, locale: :en, unit: "DKK ", separator: ",") %></span>
		      <%#= button_to 'Add to Cart', line_items_path(product_id: product) %>
		    </div>
        
		  </div>      
      
      
		 <% end %>
	<% end %>	  
<% end %>

</div>